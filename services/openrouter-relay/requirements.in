# openrouter-relay runtime dependencies (Phase 0 lock)
# Target: Python 3.12
#
# The relay is a fixed-policy adapter, not a general proxy. It needs only an
# ASGI server, request validation, and an upstream HTTPS client.
starlette>=1.3.1   # see CVE-2026-48710 note in mud-control/requirements.in
uvicorn>=0.52.4
httpx2>=2.12.0     # successor client package required by the mcp 2.x line
pydantic>=2.13.4
